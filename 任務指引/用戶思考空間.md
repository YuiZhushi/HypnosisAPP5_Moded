!!**此檔案為草稿，僅供用戶規劃用!!
禁止直接修改此檔案，除非經過用戶明確同意。
默認不讀取此檔案，除非經過用戶同意。
<!-- 分隔線 -->
<!-- 已完成筆記
角色編輯APP UI重整?
新增角色功能
地圖APP 基本完成(物品檢測 與 產業等相關功能尚未實作，需要等到相關APP完成才能實作)
將tarven和mvu解偶，專注在開發APP與玩法
-->

開發優先順序:
物品欄APP
線上商店APP
研發物品APP
地圖APP

聊天APP
私訊，各角色與用戶的私訊
群聊，

身體改造APP

產業APP
有妓院、學院、工廠、農場
妓院玩法要透過互動訓練來提升妓女的技巧，有時候也會有失敗的互動，接客的選擇由玩家分配，不同的妓女對不同的顧客有不同的好感度與技巧需求與價格。訓練會消耗時間與金錢，也會有失敗的風險，失敗則技巧不會提升。
甚至有可能發生因為妓女被操爽了，導致承諾顧客不用付錢之類的。

匿名版APP重構
討論區，區分成幾個板塊，個人成果展示、求助區、閒聊區
閒聊區:
求助區:
個人成果展示:
帖子組成，

幫助APP UI

新增成就

內部AI填寫邏輯修改

後端邏輯連接與修改

日曆APP可由AI透過MVU新增事件、修改事件、刪除事件。
<!-- 工作中提詞 -->
現在要做幫助APP 的UI重整
核心資源說明
主要玩法說明
催眠APP使用說明
匿名版APP使用說明
日曆APP使用說明
設定APP使用說明

---

目前的所有APP與未來的發展計畫應該有很多需要使用同一個資料結構的地方，例如:
背景資料(時間、天氣等等)
玩家資源(主角有甚麼的裝備、道具、各種屬性狀態等等)
NPC資料(NPC有甚麼的裝備、道具、各種屬性狀態等等)
NPC人設(NPC在故事中的個性、背景等等，不太會變化)
地點資料(地點的屬性、狀態等等)

而其它APP都需要利用這些資料來判斷或顯示、使用或執行功能。例如:
物品欄APP需要利用物品資料來顯示玩家目前擁有的物品。
角色系統需要利用物品資料來判斷玩家是否可以穿戴某件裝備。
地圖APP需要利用玩家資料來顯示玩家目前所處的地點。
地圖APP需要利用玩家資料、NPC資料、地點資料來判斷各種條件判定。
產業APP需要利用玩家資料、NPC資料、地點資料來判斷各種條件判定。
研發物品APP需要利用玩家資料、NPC資料、地點資料來判斷各種條件判定。

幫我找找看有沒有需要的各種共同資料結構，規劃中的APP需要的也要考慮進去，例如妓院APP需要玩家資源、地點資源與NPC資源來判斷誰有可以當妓女、玩家是否已經解鎖妓院等等，應該會有很多可以共通使用。

---

我想加入一個新的APP，為地圖APP，但這很有難度，因為相當於是要在文字故事且故事發展不確定的情況下，利用文字處理新增地點、修改地點、地點資訊、同步所處地點、繪製地圖等等，這有很大的難度。
我需要一些靈感來構造這個系統，讓他可以適應閣樓層的劇情發展與不確定性

---
現在要重做${}的UI
舊的UI可以完全捨棄。先將舊的UI先歸檔到 @Archive/ui 並且用做重作UI時的參考。

類似催眠APP UI層，先使用模擬資料及模擬後端，使用模擬後端的API。
ui的風格要和催眠APP的UI風格一致。
等完成後再將API連接接入真實後端(先不做，等其他UI也都做好，我會再手動要求)。

UI程式碼要註釋大的UI區塊。

---

現在要**仔細****反覆**找找看 所有的App 裡還有沒有類似的使用舊方法與值，或者不使用載入的數值的問題

---
接著要做各大小介面的兼容性。
我發現fetch模型或儲存API設定都會馬上重整UI，有沒有方法可以改善。
<!-- 其他備用提詞 -->
<!--

---

我發現有幾種比較主要的變數操作:
直接查詢MVU的變數
直接查詢Chat的變數
根據MVU變數查詢Chat中的某個資料

直接改變MVU變數
直接改變Chat變數
根據MVU變數更改Chat中的某個資料

例如:
直接查詢MVU的變數:查詢玩家的基礎資源、查詢現在時間、查詢玩家所處位置的id。
直接查詢Chat的變數:查詢所有目前存在的地點(不管有沒有發現)、查詢所有催眠技術(不管有沒有學會)、查詢某個物品的詳細資料。
根據MVU變數查詢Chat中的某個資料:查詢玩家的某個物品的詳細資料(根據玩家目前所在物品欄的物品id在staticData中尋找)、查詢NPC擁有的物品的詳細訊息(根據NPC目前所在物品欄的物品id在staticData中尋找)。

直接改變MVU變數:修改玩家的基礎資源、修改玩家所處位置的id。
直接改變Chat的變數:修改某個物品的詳細資料、修改催眠的詳細資料。
根據MVU變數更改Chat中的某個資料:修改某個物品的詳細資料(根據玩家目前所在物品欄的物品id在staticData中尋找)、修改NPC擁有的物品的詳細訊息(根據NPC目前所在物品欄的物品id在staticData中尋找)。
---

我現在想將開發用資料結構進行調整:
staticData改成是模擬世界書讀取並解析後的變數。
database改成分為兩個檔案，一個模擬在iframe載入後用戶進行操作所新增或修改的聊天變數，另一個模擬在iframe載入後用戶進行操作所新增或修改的MVU變數。

iframe啟動時載入流程改成:
先將staticData內的各個變數載入成模擬的chat變數與MVU變數。
接著載入database中的各個變數將其中與已經載入的變數相同的部分覆蓋掉(覆蓋順序為database > staticData)。

然後api要拿變數時，改為通過模擬的chat與MVU變數，編輯時也對這兩個變數進行操作。

可以想到目前的API內有很多對變數的操作沒有分出chat與mvu，所以找出所有的對變數的操作，並進行分類，有疑問問我。

---

現在是要對行為邏輯的編輯頁面做改進，現在遇見了幾個問題:
由於行為分支的結構比較複雜，是分為四個固定屬性，每個屬性下有階段區分，每個階段又有許多的條件與內容。
這導致了UI的層級過深，使得用戶在手機端或小螢幕上很難看清楚目前結構與快速新增與修改。
目前是ejs下套入yaml的方式。
所以要想一個方式能讓這結構變的更清楚，且容易新增與修改。
先準備計畫，不要直接修改。

---
現在要重做設定APP的UI
---
我發現節點位置的算法有點問題，出現了線路重疊的錯誤。
但我覺得雖然可以避免線路重疊，但是會把線路拉的太長，有沒有更好的辦法。
先準備計畫，不要直接修改。

我覺得可能得保持目前UI的風格，但OS與各APP的UI與調用流程要重新設計與調整了
---

接下來應該進行甚麼任務，提示詞為何?
---

我要怎麼檢驗這些改動會不會影響目前行為

---

檢查一下項目目前的情況
是否有層級臃腫、名稱混淆、名稱指向性等等
是否有邏輯跨層調用、邏輯分散、轉發路徑複雜、過度封裝等等
是否有過度分層、層級不明確等等
是否有同功能重複實做等等

---
自訂任務投入金錢使用數字輸入欄，獎勵分配也使用數字輸入欄。
完成條件設定全域資源缺了好幾項，角色條件也缺了很多。
---
現在要重做成就與任務APP的UI
需要類似催眠APP UI層的模擬資料及後端。
等完成後再將API連接接入真實後端。
先將舊的UI移到歸檔供參考。

AchievementDef (成就定義：ID、名稱、描述、獎勵 PT、解鎖條件說明)。
QuestDef (任務定義：ID、名稱、描述、獎勵、狀態)。
MockUserData (包含玩家目前擁有的資源、已解鎖/已領取的成就、進行中/已完成的任務狀態)。

成就與任務資料結構:
id: ${text}
名稱:${text}
資料類型: ${achievement}|${quest}
完成條件:
  類型: ${由程式判定}|${由AI判定}
  完成條件: ${condition}|${text}
獎勵:
  類型: ${money}|${mcEnergyLimitIncrease}|${mcPoints}
  數量: ${number}

runtime:
export interface MockUserData { // 模擬可以從後端獲取的資料，將來可以替換成真正的後端資料接口
  userName: string;
  // A. 基礎資源與訂閱資訊
  money: number;
  mcEnergy: number;
  mcEnergyMax: number;
  mcPoints: number; // 持有催眠點(PTS)
  totalConsumedMc: number;
  vipTier: number; // 0~5
  vipEndVirtualMinutes: number; // 虛擬分鐘
  vipAutoRenew: boolean;
  suspicion: number; // 主角可疑度

  // B. 玩家持有的多重物件 (Record 結構)
  ownedHypnosis: Record<string, { enabled: boolean, settings?: any }>;
  ownedEquipments: Record<string, { enabled: boolean, settings?: any }>;
  ownedCombos: Record<string, { enabled: boolean, settings?: any }>;

  // C. 成就與任務
  claimedQuests: Record<string, {isCompleted: boolean,}>; // 是否已領取
  completedAchievements: idstring[]; // 是否已完成
}
---
獎勵種類|獲得數量|消耗分配值
金錢|1000|1
MC能量|10|1
MC能量最大值|1|1
MC點|1|1
可疑度|-0.2|1(向下取整)
---
程式判斷條件可以有多個Target與多個Condition
如果沒有分配完分配值，在發布時會自動返還，並且會標註將多餘分配值換成金錢自動返還持有金錢中
如果分配值不夠，發布前會檢查持有金錢能不能補足缺少的分配點，如果可以發布按鈕的樣式需要變化，變成可用金錢補足的樣式，不能則保持不能發布的樣式，如果足夠直接發布則顯示足夠發布的樣式。
---
UI 設計 ：始分次實作 UI 頁面：
頂部區塊：狀態欄、用戶資訊、資源顯示面板、VIP狀態與升級面板。
設備管理區：設備列表展示、購買介面、啟用/停用切換。
催眠使用區：快捷組合、已解鎖催眠列表、發送設定面板。
催眠管理/商店區：購買新催眠、自製催眠面板。

-->
