<${角色名字}人设>
${角色名字}:
  title: ${稱號/頭銜}
  gender: ${性別}
  age: ${年齡}
  identity:
    public: ${公開身份描述}
    hidden: ${隱藏身份描述}
  social connection:
    ${關聯人物A}:
      relationship: ${關係描述}
    ${關聯人物B}:
      relationship: ${關係描述}
  personality:
    core:
      ${核心性格1}: ${具體描述}
      ${核心性格2}: ${具體描述}
    conditional:
      ${條件性格1}: ${具體描述}
    hidden:
      ${隱藏性格1}: ${具體描述}
  habit:
    - ${習慣動作/日常小特徵1}
    - ${習慣動作/日常小特徵2}
  hidden behavior:
    - ${隱密行為1}
    - ${隱密行為2}
  appearance:
    height: ${身高}
    weight: ${體重}
    measurement: ${三圍}
    style: ${穿搭風格總結}
    overview: ${長相與外貌總覽}
    attire:
      school: ${學校裝束/制服穿著方式}
      casual: ${日常便服裝束}
    feature:
      - ${身體小特徵1}
      - ${身體小特徵2}
  sexual preference:
    masturbation frequency: ${自慰頻率描述}
    orgasm response: ${高潮時的特定反應}
    sensitive spot:
      - ${敏感帶1}（${觸碰時的反應}）
      - ${敏感帶2}
    fetish:
      - ${性癖1}：${具體表現描述}
      - ${性癖2}：${具體表現描述}
    special trait:
      - ${性方面特殊特徵1}
      - ${性方面特殊特徵2}
  weakness:
    - ${弱點/害怕的事物1}
    - ${弱點/害怕的事物2}
</${角色名字}人设>


<${角色名字}行为指导>
${角色名字}现在的性欲: {{get_message_variable::stat_data.角色.${角色名字}.发情值}}
<%_ if (getvar('stat_data.角色.${角色名字}.发情值') < 20) { _%>
  行为指导:
    - ${發情值 < 20 時的行為描述}
<%_ } else if (getvar('stat_data.角色.${角色名字}.发情值') < 40) { _%>
  行为指导:
    - ${發情值 20~39 時的行為描述}
<%_ } else if (getvar('stat_data.角色.${角色名字}.发情值') < 60) { _%>
  行为指导:
    - ${發情值 40~59 時的行為描述}
<%_ } else if (getvar('stat_data.角色.${角色名字}.发情值') < 80) { _%>
  行为指导:
    - ${發情值 60~79 時的行為描述}
<%_ } else if (getvar('stat_data.角色.${角色名字}.发情值') < 95) { _%>
  行为指导:
    - ${發情值 80~94 時的行為描述}
<%_ } else if (getvar('stat_data.角色.${角色名字}.发情值') >= 95) { _%>
  行为指导:
    - ${發情值 >= 95 時的行為描述}
<%_ } _%>
---
${角色名字}对<user>的态度:
  associated variable: 警戒度({{get_message_variable::stat_data.角色.${角色名字}.警戒度}})
  <%_ if (getvar('stat_data.角色.${角色名字}.警戒度') > 99) { _%>
  ${狀態標題(可選)}:
    行为指导:
      - ${極高警戒 (>99) 狀態下的行為指導}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.警戒度') > 79) { _%>
  ${狀態標題(可選)}:
    行为指导:
      - ${高警戒 (80~99) 狀態下的行為指導}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.警戒度') > 59) { _%>
  ${狀態標題(可選)}:
    行为指导:
      - ${中高警戒 (60~79) 狀態下的行為指導}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.警戒度') > 39) { _%>
  ${狀態標題(可選)}:
    行为指导:
      - ${中低警戒 (40~59) 狀態下的行為指導}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.警戒度') > 19) { _%>
  ${狀態標題(可選)}:
    行为指导:
      - ${低警戒 (20~39) 狀態下的行為指導}
  <%_ } else { _%>
  ${狀態標題(可選)}:
    行为指导:
      - ${無警戒 (<= 19) 狀態下的行為指導}
  <%_ } _%>
---
${角色名字}对{{user}}的好感度: {{get_message_variable::stat_data.角色.${角色名字}.好感度}}
  <%_ if (getvar('stat_data.角色.${角色名字}.好感度') < 20) { _%>
    行为指导:
      - ${低好感度 (<20) 時的行為描述}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.好感度') < 40) { _%>
    行为指导:
      - ${中低好感度 (20~39) 時的行為描述}
    变化倾向:
      - ${變化描述(可選)}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.好感度') < 60) { _%>
    行为指导:
      - ${普通好感度 (40~59) 時的行為描述}
    变化倾向:
      - ${變化描述(可選)}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.好感度') < 80) { _%>
    行为指导:
      - ${高好感度 (60~79) 時的行為描述}
  <%_ } else { _%>
    行为指导:
      - ${極高好感度 (>=80) 時的行為描述}
  <%_ } _%>
---
${角色名字}对{{user}}的服从度: {{get_message_variable::stat_data.角色.${角色名字}.服从度}}
  <%_ if (getvar('stat_data.角色.${角色名字}.服从度') < 20) { _%>
    行为指导:
      - ${低服從度 (<20) 時的行為描述}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.服从度') < 40) { _%>
    行为指导:
      - ${較低服從度 (20~39) 時的行為描述}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.服从度') < 60) { _%>
    行为指导:
      - ${普通服從度 (40~59) 時的行為描述}
    变化倾向:
      - ${變化描述(可選)}
  <%_ } else if (getvar('stat_data.角色.${角色名字}.服从度') < 80) { _%>
    行为指导:
      - ${高服從度 (60~79) 時的行為描述}
    变化倾向:
      - ${變化描述(可選)}
  <%_ } else { _%>
    行为指导:
      - ${極高服從度 (>=80/完全臣服) 時的行為描述}
  <%_ } _%>
rule:
  - 行为指导优先于作为背景的`角色关键信息`和`角色详情`
  - 好感度和服从度行为可以混合
  - 角色的好感与服从度要优先于警戒度, 只要好感度或服从度大于警戒度, 就不会触发警戒
</${角色名字}行为指导>
